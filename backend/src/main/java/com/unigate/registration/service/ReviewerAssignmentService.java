package com.unigate.registration.service;

import org.springframework.stereotype.Service;

/** Reviewer role removed — this class is kept as an empty stub to avoid compilation errors on stale imports. */
@Service
public class ReviewerAssignmentService {
}
